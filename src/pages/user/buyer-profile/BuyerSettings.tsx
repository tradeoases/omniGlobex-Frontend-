import { Outlet } from "react-router-dom";

const BuyerSettings = () => {
  return (
    <div>
      {/* Placeholder for nested routes */}
      <Outlet />
    </div>
  );
};

export default BuyerSettings;
