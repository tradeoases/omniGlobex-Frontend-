import { Outlet } from "react-router-dom";

const BuyerAccount = () => {
  return (
    <div>
      {/* Placeholder for nested routes */}
      <Outlet />
    </div>
  );
};

export default BuyerAccount;
